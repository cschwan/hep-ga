# depend on Doxyfile generated by configure
doxygen-stamp: Doxyfile
	$(DOXYGEN) $<
	echo "timestamp for Doxyfile" > $@

CLEANFILES = doxygen-stamp

# add documentation target to the default one
all-local: doxygen-stamp

# when cleaning, remove html docs as well
clean-local:
	rm -rf html
